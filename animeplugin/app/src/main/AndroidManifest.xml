<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
        Declares the stream_search capability. Once installed, Lumora discovers this plugin
        and shows a "Find stream" button on channels. Search queries go to AniList for anime
        metadata; stream resolution goes through Senshi (direct HLS API).
        -->
        <service
            android:name=".StreamService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.lumora.action.PLUGIN" />
            </intent-filter>
            <meta-data
                android:name="lumora.plugin.id"
                android:value="com.lumora.animeplugin" />
            <meta-data
                android:name="lumora.plugin.description"
                android:value="Search anime on AniList and stream from Senshi, MegaPlay, and other anime providers" />
            <meta-data
                android:name="lumora.plugin.capabilities"
                android:value="stream_search" />
        </service>
    </application>
</manifest>
